# Public repo policy

This repo is public on GitHub. Applies to every commit, branch, PR, and issue:

- Never commit secrets, tokens, API keys, or personal data (including in example/test fixtures).
- Never include real names/emails in code comments or commit messages beyond standard git authorship.
- Treat everything written here — commit messages, code comments, README — as something any stranger can read today.
