name: scroll_wheel_date_picker
description: "A Flutter package for a scrollable and flat perspective wheel-type date picker like in TikTok."
version: 0.1.0

homepage: https://github.com/Fingertips18/scroll-wheel-date-picker
repository: https://github.com/Fingertips18/scroll-wheel-date-picker

environment:
  sdk: ">=3.2.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ">=0.19.0 <1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=2.0.0 <7.0.0"

flutter:

topics:
  - ui
  - datepicker
  - widgets
  - scroll
  - wheel
